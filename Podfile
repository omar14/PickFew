# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PickFew' do
  use_frameworks!
  pod 'Fabric'
  pod 'Crashlytics'

end

target ‘Pick’ do
  use_frameworks!
  pod 'Fabric'
  pod 'Crashlytics'

end
